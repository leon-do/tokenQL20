{
  "name": "tokenql-erc20",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ tokenql-20",
    "create-local": "graph create --node http://localhost:8020/ tokenql",
    "remove-local": "graph remove --node http://localhost:8020/ tokenql",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 tokenql"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
