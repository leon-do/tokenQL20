{
  "name": "erc20-tokenQL",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ erc20-tokenQL",
    "create-local": "graph create --node http://localhost:8020/ erc20-tokenQL",
    "remove-local": "graph remove --node http://localhost:8020/ erc20-tokenQL",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 erc20-tokenQL"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
